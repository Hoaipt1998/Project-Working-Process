import React from 'react';

import {CartContext} from '../components/Cart';
function ProductPage() {
    return (
        <div>
            ProductPage2
            {/* <CartContext.Consumer>
                {
                    ({ addToCart}) => <button onClick={() => addToCart("1")} > Add to cart</button>
                

                }
            </CartContext.Consumer> */}
          
        </div>
    );
  }
  
  export default ProductPage;